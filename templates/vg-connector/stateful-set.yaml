{{- if .Values.vgConnector.enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: vg-connector
  namespace: {{ $.Release.Namespace | quote }}
  labels:
    app: vg-connector
    owner-team: echo
  annotations:
    "cognigy.com/source-code": "git@github.com:Cognigy/vg-connector.git"
    "cognigy.com/source-code.drachtio": "git@github.com:Cognigy/vg-drachtio.git"
spec:
  serviceName: vg-connector-headless
  selector:
    matchLabels:
      app: vg-connector
  replicas: {{ .Values.vgConnector.replicaCount }}
  template:
    metadata:
      labels:
        app: vg-connector
      annotations:
        checksum/cognigy-env: {{ include (print $.Template.BasePath "/configurations/cognigy-env.yaml") $ | sha256sum }}
    spec:
      {{- include "image.pullSecrets" $ | nindent 6 }}
      {{- if .Values.vgConnector.affinity }}
      affinity: {{- include "vg.common.tplvalues.render" (dict "value" .Values.vgConnector.affinity "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.vgConnector.nodeSelector }}
      nodeSelector: {{- include "vg.common.tplvalues.render" (dict "value" .Values.vgConnector.nodeSelector "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.vgConnector.priorityClassName }}
      priorityClassName: {{ .Values.vgConnector.priorityClassName }}
      {{- end }}
      {{- if .Values.vgConnector.podSecurityContext }}
      securityContext: {{- include "vg.common.tplvalues.render" (dict "value" .Values.vgConnector.podSecurityContext "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.vgConnector.tolerations }}
      tolerations: {{- include "vg.common.tplvalues.render" (dict "value" .Values.vgConnector.tolerations "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.vgConnector.terminationGracePeriodSeconds }}
      terminationGracePeriodSeconds: {{ .Values.vgConnector.terminationGracePeriodSeconds }}
      {{- end }}
      restartPolicy: Always
      containers:
        - name: vg-connector
          image: {{ include "vg.common.image.render" (dict "global" $.Values.global "image" .Values.vgConnector.image) }}
          imagePullPolicy: {{ .Values.vgConnector.imagePullPolicy }}
          resources: {{- toYaml .Values.vgConnector.resources | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.vgConnector.port }}
              protocol: TCP
            - name: health
              containerPort: {{ .Values.health.port }}
              protocol: TCP
            - name: metrics
              containerPort: {{ .Values.metrics.port }}
              protocol: TCP
          lifecycle:
            preStop:
              exec:
                command: ["sleep", "5"]
          livenessProbe:
            # No initialDelaySeconds: startupProbe below already blocks this
            # probe from running at all until /health/startup succeeds, so an
            # extra fixed delay here only adds latency to detecting a genuine
            # post-boot hang, without protecting anything during startup.
            httpGet:
              path: /health/liveness
              port: {{ .Values.health.port }}
            failureThreshold: 3
            periodSeconds: 60
            timeoutSeconds: 10
          startupProbe:
            httpGet:
              path: /health/startup
              port: {{ .Values.health.port }}
            failureThreshold: 30
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            # startupProbe succeeding does NOT by itself gate this pod's Ready
            # condition or its per-ordinal Service membership (traefik-services.yaml):
            # kubelet only withholds traffic from a pod when a readinessProbe is
            # present at all (pkg/kubelet/prober/prober_manager.go computes
            # `ready = !exists` when none is configured), and a container is
            # marked Running by the container runtime the moment its entrypoint
            # is exec'd — well before main.ts finishes connecting to Redis/
            # drachtio and mounting the ACS/Teams routes. Reusing the
            # startupProbe's endpoint is enough: kubelet won't evaluate this
            # probe until startupProbe first succeeds anyway, but this probe's
            # mere existence is what withholds traffic until that happens.
            httpGet:
              path: /health/startup
              port: {{ .Values.health.port }}
            failureThreshold: 3
            periodSeconds: 10
            timeoutSeconds: 5
          env:
            {{- /* Reloadable configs were introduced in v2026.17.0; once this service reads COGNIGY_AI_VERSION from the mounted ConfigMap, this fallback can be removed in a future release. */}}
            {{- if not .Values.global.reloadableConfig.enabled }}
            - name: COGNIGY_AI_VERSION
              value: {{ regexFind "^[0-9]+\\.[0-9]+\\.[0-9]+" .Chart.Version | quote }}
            {{- end }}
            - name: ENVIRONMENT
              value: {{ .Values.tracing.environment | quote }}
            - name: JAMBONES_MYSQL_DATABASE
              value: {{ .Values.mysql.database.name | quote }}
            - name: JAMBONES_MYSQL_HOST
              value: {{ .Values.mysql.host }}
            - name: JAMBONES_MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: voicegateway-mysql-password
                  key: mysql-password
            - name: JAMBONES_MYSQL_USER
              value: {{ .Values.mysql.auth.username | quote }}
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: voicegateway-jwt
                  key: jwt
            - name: CONNECTOR_BASE_URL
              value: {{ printf "%s://%s" (ternary "https" "http" .Values.tls.enabled) .Values.ingress.vgConnector.host | quote }}
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: DRACHTIO_SECRET
              valueFrom:
                secretKeyRef:
                  name: voicegateway-drachtio-secret
                  key: secret
            ## Genesys AudioHook master switch — uniform env name across all
            ## three services so the helm chart fans out one value to one name
            ## everywhere. vg-connector PR #35 renames the source to read this.
            - name: ENABLE_GENESYS_AUDIOHOOK
              value: {{ .Values.global.enableGenesysAudioHook | quote }}
            ## MS Teams master switch — mirrors enableGenesysAudioHook: one
            ## global flag flipped per environment, no per-module gating. The
            ## connector reads ENABLE_MICROSOFT_TEAMS to mount the Teams module.
            - name: ENABLE_MICROSOFT_TEAMS
              value: {{ .Values.global.enableMsTeams | quote }}
            - name: MSTEAMS_FEATURE_RECORD_AUDIO
              value: {{ default "false" .Values.vgConnector.msTeams.recordAudio | quote }}
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: SBC_BASE_URL
              {{- if .Values.sbc.sip.useStatefulSet }}
              value: {{ .Values.sbc.sip.headlessServiceName | quote }}
              {{- else }}
              value: {{ .Values.sbc.sip.uri | quote }}
              {{- end }}
            - name: NODE_ENV
              value: {{ .Values.global.nodeEnv | quote }}
            - name: PORT
              value: {{ .Values.vgConnector.port | quote }}
            - name: SIGTERM_FORCE_CLOSE_TIMEOUT_MS
              value: {{ .Values.vgConnector.sigtermForceCloseTimeoutMs | quote }}
            {{- if .Values.vgConnector.extraEnvVars }}
            {{- include "vg.common.tplvalues.render" (dict "value" .Values.vgConnector.extraEnvVars "context" $) | nindent 12 }}
            {{- end }}
          volumeMounts:
            - name: cognigy-env-reloadable
              mountPath: /config/env
              readOnly: true
          envFrom:
            - configMapRef:
                name: cognigy-env
        - name: drachtio
          image: {{ include "vg.common.image.render" (dict "global" $.Values.global "image" .Values.drachtio.image) }}
          imagePullPolicy: {{ .Values.drachtio.imagePullPolicy }}
          resources: {{- toYaml .Values.drachtio.resources | nindent 12 }}
          args:
            - drachtio
            - --loglevel
            - {{ if .Values.drachtio.logLevel }}{{ .Values.drachtio.logLevel | quote }}{{ else }}{{ .Values.global.logLevel | quote }}{{ end }}
            - --sofia-loglevel
            - {{ .Values.vgConnector.sofiaLoglevel | quote }}
          env:
            - name: DRACHTIO_SECRET
              valueFrom:
                secretKeyRef:
                  name: voicegateway-drachtio-secret
                  key: secret
            - name: DRACHTIO_SUPPORT_BEST_EFFORT_TLS
              value: {{ if .Values.drachtio.supportBestEffortTls }}{{ print 1 | quote }}{{ else }}{{ print 0 | quote }}{{ end }}
            - name: PRIVATE_IP_ONLY
              value: {{ .Values.drachtio.privateIpOnly | quote }}
            - name: SOFIA_SEARCH_DOMAINS
              value: "1"
            - name: SOFIA_SRES_NO_CACHE
              value: "1"
            {{- if .Values.drachtio.extraEnvVars }}
            {{- include "vg.common.tplvalues.render" (dict "value" .Values.drachtio.extraEnvVars "context" $) | nindent 12 }}
            {{- end }}
      volumes:
        - name: cognigy-env-reloadable
          configMap:
            name: cognigy-env-reloadable
            items:
              - key: cognigy.yaml
                path: cognigy.yaml
{{- end }}
