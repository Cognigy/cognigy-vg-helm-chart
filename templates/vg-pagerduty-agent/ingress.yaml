{{- if and .Values.vgPagerdutyAgent.enabled .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vg-pagerduty-agent
  namespace: {{ $.Release.Namespace | quote }}
  labels:
    owner-team: echo
  annotations:
  {{- if eq .Values.ingress.ingressClassName "traefik" }}
    {{- if .Values.tls.enabled }}
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    {{- else }}
    traefik.ingress.kubernetes.io/router.entrypoints: web
    {{- end }}
    {{- if .Values.ingress.vgPagerdutyAgent.ipWhiteListMiddleware.enabled }}
    traefik.ingress.kubernetes.io/router.middlewares: {{ $.Release.Namespace }}-ipwhitelistvgpagerdutyagent@kubernetescrd
    {{- end }}
  {{- end }}
  {{- if .Values.ingress.vgPagerdutyAgent.extraAnnotations }}
    {{- include "vg.common.tplvalues.render" (dict "value" .Values.ingress.vgPagerdutyAgent.extraAnnotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.ingressClassName }}
  {{- include "vg.tlsCertificate.secretName.render" $ | nindent 2 }}
  rules:
    - host: {{ .Values.ingress.vgPagerdutyAgent.host }}
      http:
        paths:
          - backend:
              service:
                name: vg-pagerduty-agent
                port:
                  number: {{ .Values.vgPagerdutyAgent.port | default 3000 }}
            path: /
            pathType: Prefix
{{- end }}
