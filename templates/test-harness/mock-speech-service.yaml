{{- if .Values.vgTestHarness.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: mock-speech
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: mock-speech
    owner-team: echo
spec:
  type: ClusterIP
  selector:
    app: mock-speech
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
{{- end }}
