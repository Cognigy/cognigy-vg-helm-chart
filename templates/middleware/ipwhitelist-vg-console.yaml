{{- if and (.Values.ingress.enabled) (.Values.ingress.vgConsole.ipWhiteListMiddleware.enabled) }}
{{- with .Values.ingress.vgConsole.ipWhiteListMiddleware }}
---
# Whitelisting Based on `X-Forwarded-For` with `depth=0`
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: ipwhitelistvgconsole
  namespace: {{ $.Release.Namespace | quote }}
spec:
  ipAllowList: {{- toYaml .ipWhiteList | nindent 4 }}
{{- end }}
{{- end }}