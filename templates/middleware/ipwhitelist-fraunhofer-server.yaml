{{- if and (.Values.ingress.enabled) (.Values.traefik.enabled) (.Values.ingress.fraunhofer.server.ipWhiteListMiddleware.enabled) }}
{{- with .Values.ingress.fraunhofer.server.ipWhiteListMiddleware }}
---
# Whitelisting Based on `X-Forwarded-For` with `depth=0`
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: ipwhitelistfraunhoferserver
  namespace: {{ $.Release.Namespace | quote }}
spec:
  ipWhiteList: {{- toYaml .ipWhiteList | nindent 4 }}
{{- end }}
{{- end }}